
import { Box, Text } from '@raid/basic-kit'
// import { NavLink } from '../../components/link'
// import { useRouter } from 'next/router'
// import { mutate } from 'swr'

/**
 * see https://github.com/zeit/swr#mutate-based-on-current-data
 * Will likely have to use mutate to add a list of resources, then in the [id]
 * pages pull/set from/to that list.
 */

const SearchIndex = () => {
  // const router = useRouter()

  return (
    <Box>
      <Text>@TODO</Text>
    </Box>
  )
}

export default SearchIndex
